apiVersion: v1
kind: ConfigMap
metadata:
    name: app-config
    namespace: {{ .Values.namespace.name }}
    labels: 
        {{- include "php-app.labels" . | nindent 6 }}
data:
    APP_ENV: {{ .Values.config.appEnv }}