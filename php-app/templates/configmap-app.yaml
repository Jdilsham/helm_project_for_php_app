apiVersion: v1
kind: ConfigMap
metadata:
    name: app-config
    namespace: {{ .Values.namespace.name }}
    labels:
        app: {{ include "php-app.labels" . | nindent 8 }}
data:
    APP_ENV: {{ .Values.config.appEnv }}